import type { Config } from "tailwindcss";

export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ['"fustat"', "sans-serif"],
        fustat: ['"fustat"', "sans-serif"],
      },
    },
  },
} satisfies Config;
